package org.events

import org.events.Event

class FHE(
    title: String,
    description: String,
    date: String,
    time: String,
    address: String) : Event(title, description, date, time, address) {
}
